# Amane Stock Manager 0.2.3

- 修复迁移后“可以登录和读取库存列表，但同步提示权限或会话校验失败”的问题。API 仍连接 `api.amaneacg.space`，管理员请求来源改为服务端独立配置的 `console.amaneacg.space`；继续携带原有会话和 CSRF 校验。
- 对齐服务端权限规则：支持独立库存管理权限，并兼容旧内容管理权限；商店改价仍要求定价权限。
- 区分权限不足、来源/会话校验失败和业务定价权限错误，避免所有 403 都显示为同一句模糊提示。
- 加强测试中的真实来源校验，保留 0.2.2 的价格联动、待发队列、断网恢复与冲突保护。

通过应用内“检查更新”安装。本次沿用迁移后的加密登录存储，保留本地库存和待同步请求，不需要重新创建云端库存。升级后可点击“立即同步 / 重试”；如会话已过期，再退出并重新登录。
